<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kr.co.skills">

    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Skills">

        <activity
            android:name=".skill.MaterialDesign3Activity"
            android:exported="false"/>

        <activity
            android:name=".skill.HeroAnimActivity"
            android:exported="false" />

        <activity
            android:name=".skill.PhotoSaveActivity"
            android:exported="false" />

        <activity
            android:name=".skill.MaterialDesign2Activity"
            android:theme="@style/Theme.Skills_Status_Black" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>